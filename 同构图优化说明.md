# 同构图性能优化方案

## 问题现状

- **异构图**（ACM/DBLP/IMDB）：F1 = 69-75% ✅ 效果良好
- **同构图**（com-amazon 等）：F1 = 2.55% ❌ 效果极差
- **SLRL 基线**：com-amazon F1 = 87.10%

## 核心差异分析

### 异构图的优势
1. **多关系类型**：不同类型的边携带不同语义（如 ACM 的 pap, psp, pcp）
2. **丰富的节点特征**：原生的节点属性（论文特征、作者特征等）
3. **元路径**：可以定义多种语义路径，捕捉不同关系
4. **关系融合**：ICRA 机制可以融合多种关系的信息

### 同构图的挑战
1. **单关系类型**：只有一种边类型，信息维度单一
2. **缺乏节点特征**：只能使用结构特征（度、PageRank 等）
3. **社区结构复杂**：大规模社交网络的社区边界模糊
4. **对比学习困难**：缺乏语义信息，正负样本区分度低

## 优化方案（已实施）

### 1. 调整对比学习温度参数 ⭐ 最重要

**问题**：同构图的嵌入分布与异构图不同，需要更尖锐的分布

**优化**：
- 异构图：`tau = 0.4`（默认）
- 同构图：`tau = 0.25`（更低的温度）

**原理**：更低的温度使 softmax 分布更尖锐，增强节点之间的区分度

### 2. 增加候选边数量和来源

**问题**：同构图的重构视图不够有效

**优化**：
- 候选边数量：从 5 增加到 10-15
- 候选边来源：`embed,twohop,common`（多种来源融合）
- 候选边标签模式：`soft`（软标签）

**原理**：更多的候选边可以捕捉更多潜在的社区结构

### 3. 调整损失权重

**问题**：同构图需要更强的对比学习信号

**优化**：
- 对比学习权重：保持默认
- 候选边重构损失：从 0 增加到 0.1
- 对抗特征保持损失：从 0.1 降低到 0.05
- 对抗损失权重：从 1.0 增加到 1.2

**原理**：增强对比学习，适当降低重构难度

### 4. 优化边权重生成

**问题**：同构图的边权重需要更尖锐的分布

**优化**：
- 对抗温度：从 1.0 降低到 0.8
- 偏置项：从 0.0001 增加到 0.001

**原理**：使边权重分布更尖锐，增强边的区分度

### 5. 数据集特定的超参数

针对不同数据集的特点进行了微调：

| 数据集 | 隐藏层维度 | 候选边数量 | Mini-batch |
|--------|-----------|-----------|-----------|
| com-amazon | 256 | 15 | ❌ |
| com-dblp | 256 | 10 | ❌ |
| com-youtube | 128 | 10 | ✅ |
| com-twitter | 128 | 10 | ✅ |
| com-livejournal | 128 | 10 | ✅ |

## 使用方法

### 快速测试（推荐）

```bash
# 使用优化配置测试 com-amazon
python test_homogeneous_opt.py --dataset com-amazon --encoder gcn --num_epochs 200

# 使用优化配置测试 com-dblp
python test_homogeneous_opt.py --dataset com-dblp --encoder gcn --num_epochs 200
```

### 原始命令（对比）

```bash
# 原始配置（未优化）
python train_ig.py --dataset com-amazon --encoder gcn --num_hidden 256 --num_epochs 200
```

## 预期效果

### 优化前
- com-amazon: F1 ≈ 2.55%
- 社区搜索效果差，无法有效识别社区结构

### 优化后（预期）
- com-amazon: F1 ≈ 15-30%（提升 6-12 倍）
- 社区边界更清晰
- 对比学习效果显著改善

### 与 SLRL 的差距
- SLRL: 87.10%
- 优化后: 15-30%
- 差距仍然较大，主要因为：
  - SLRL 使用强化学习，专门针对社区搜索优化
  - SLRL 使用 PPR（Personalized PageRank）等专门的图算法
  - 我们的方法是通用的对比学习框架，不是专门为同构图设计

## 进一步优化的方向

### 短期（1-2 周）
1. **社区感知的正负样本**：利用已知的社区标签构造更准确的正样本对
2. **结构洞识别**：识别不同社区之间的桥接节点作为难负样本
3. **动态难度采样**：根据训练进度动态调整正负样本的难度

### 中期（1 个月）
1. **同构图特定的边重要性**：基于共同邻居、Jaccard 相似度等结构特征
2. **模块度优化**：将模块度作为社区质量的评估指标
3. **多层次对比学习**：在不同粒度（节点、社区、图）上进行对比

### 长期（2-3 个月）
1. **专门针对同构图的模型架构**：设计更适合单关系图的编码器
2. **自监督预训练**：在大规模同构图上进行预训练
3. **迁移学习**：从异构图迁移知识到同构图

## 实验记录

请在 `best_per/experiment_log.md` 中记录优化后的实验结果：

```markdown
## com-amazon (优化后)
- 日期：2026-09-XX
- 配置：tau=0.25, num_cand=15, cand_sources=embed,twohop,common
- Micro-F1: XX.XX±X.XX
- Macro-F1: XX.XX±X.XX
- Community Search F1: XX.XX
- 相比优化前：提升 XX.XX 个百分点
```

## 注意事项

1. **不要修改异构图代码**：所有优化通过配置参数实现，不影响异构图流程
2. **逐步验证**：先在 com-amazon 上测试，确认效果后再测试其他数据集
3. **记录实验**：详细记录每次实验的配置和结果
4. **对比基线**：与 SLRL、TransZero 等基线方法进行对比

## 总结

本次优化主要通过调整超参数来提升同构图性能，核心改进包括：
- ✅ 降低对比学习温度（tau: 0.4 → 0.25）
- ✅ 增加候选边数量和来源
- ✅ 调整损失权重
- ✅ 优化边权重生成
- ✅ 数据集特定的超参数配置

预期可以将同构图性能从 2.55% 提升到 15-30%，虽然仍低于 SLRL（87%），但是显著提升。
