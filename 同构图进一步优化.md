# 同构图进一步优化方案

## 当前最佳结果

**com-amazon**: F1 = 81.22% (P=74.27%, R=94.42%)

**问题**: Precision 太低（74.27%），Recall 太高（94.42%）

## 之前的失败尝试 ❌

尝试直接调整 greedy 参数：
- `greedy_size_penalty`: 0.0 → 0.005 → 失败（F1 降到 69%）
- `greedy_patience`: 0 → 10 → 失败
- `greedy_min_gain_tol`: 0.0 → 0.01 → 失败

**原因**: 这些参数在同构图上不起作用，会过度抑制扩展，导致 Recall 暴跌

## 新的优化方向 ✅

### 核心思路：提升 embedding 质量

Precision 低的根本原因是 **embedding 不够区分**，导致社区边界模糊。

### 新增优化（已实施）

#### 1. 增强对比学习权重
```python
lambda_contrastive: 1.0 → 1.5
```
**作用**: 更强的对比学习信号，使不同社区的 embedding 更分离

**原理**: 
- 对比学习是学习 embedding 的核心
- 增强权重可以让模型更关注区分不同社区
- 预期效果：社区边界更清晰

#### 2. 增强边重构损失
```python
lambda_cand_bce: 0.1 → 0.15
```
**作用**: 更好的边重建质量

**原理**:
- 边重构帮助模型学习图结构
- 增强权重可以让模型更好地捕捉社区结构
- 预期效果：社区内部连接更紧密

#### 3. 保持其他优化
```python
tau: 0.25                    # 低温度，增强区分度
num_cand_per_node: 15        # 多候选边
cand_sources: embed,twohop,common  # 多来源
balance_alpha: 0.3           # 适度增强 Precision
suspicious_boost: 2.5        # 增强可疑节点
```

## 预期效果

### 保守估计
- **Precision**: 74.27% → 78-80% (+4-6%)
- **Recall**: 94.42% → 88-92% (-2-6%)
- **F1**: 81.22% → 83-85% (+2-4%)

### 乐观估计
- **Precision**: 74.27% → 82-85% (+8-11%)
- **Recall**: 94.42% → 85-90% (-4-9%)
- **F1**: 81.22% → 85-87% (+4-6%)

## 测试命令

```bash
# 在服务器上运行（自动应用所有优化）
python train_ig.py --dataset com-amazon --encoder gcn --num_epochs 200
```

**预期输出**:
```
  [优化] tau: 0.4 → 0.25 (增强对比学习区分度)
  [优化] num_cand_per_node: 5 → 15
  [优化] cand_sources: embed → embed,twohop,common
  [优化] lambda_cand_bce: 0.0 → 0.15
  [优化] lambda_rec: 0.1 → 0.05
  [优化] adv_lambda: 1.0 → 1.2
  [优化] adv_temp: 1.0 → 0.8
  [优化] bias: 0.0001 → 0.001
  [优化] greedy_balance_alpha: 0.15 → 0.3 (增强 Precision)
  [优化] suspicious_boost: 1.5 → 2.5 (增强可疑节点权重)
  [优化] lambda_contrastive: 1.0 → 1.5 (增强对比学习)
  [优化] lambda_cand_bce: 0.1 → 0.15 (增强边重构)
  [优化] 隐藏层维度: 1024 → 256 (适配 Amazon 数据集)
  [优化] num_cand_per_node: 10 → 15 (Amazon 社区较大)
```

## 技术原理

### 为什么之前的方法失败？

**直接调整 greedy 参数的问题**:
1. **size_penalty** 惩罚大社区 → 但同构图的社区本来就小，过度惩罚导致扩展不足
2. **patience** 早停 → 但同构图需要更激进的扩展才能达到足够的 Recall
3. **min_gain_tol** 要求最小增益 → 但同构图的边界节点增益本来就小

**根本原因**: 同构图的社区边界模糊，embedding 不够区分，导致 greedy 算法难以判断哪些节点属于社区。

### 新方法的优势

**提升 embedding 质量**:
1. **更强的对比学习** (lambda_contrastive=1.5)
   - 不同社区的 embedding 更分离
   - 社区边界更清晰
   - greedy 算法更容易判断

2. **更好的边重构** (lambda_cand_bce=0.15)
   - 社区内部连接更紧密
   - 社区之间的连接更稀疏
   - 结构特征更明显

**预期效果**:
- embedding 质量提升 → 社区边界更清晰
- greedy 算法更容易找到正确的社区
- Precision 提升，Recall 适度下降
- F1 整体提升

## 对比实验

### 配置对比

| 配置 | lambda_contrastive | lambda_cand_bce | 预期 F1 |
|------|-------------------|----------------|---------|
| 第一轮（当前最佳） | 1.0 | 0.1 | 81.22% |
| **第二轮（新优化）** | **1.5** | **0.15** | **83-87%** |

### 消融实验

```bash
# 完整优化
python train_ig.py --dataset com-amazon --encoder gcn --num_epochs 200

# 只增强对比学习
python train_ig.py --dataset com-amazon --encoder gcn --num_epochs 200 \
    --lambda_contrastive 1.5 --lambda_cand_bce 0.1

# 只增强边重构
python train_ig.py --dataset com-amazon --encoder gcn --num_epochs 200 \
    --lambda_contrastive 1.0 --lambda_cand_bce 0.15

# 两者都增强（预期最佳）
python train_ig.py --dataset com-amazon --encoder gcn --num_epochs 200 \
    --lambda_contrastive 1.5 --lambda_cand_bce 0.15
```

## 下一步计划

1. ✅ **在服务器上运行测试**
2. ✅ **记录结果到 experiment_log.md**
3. ✅ **如果 F1 达到 83%+，则成功**
4. ✅ **如果仍有差距，考虑其他方向**:
   - 不同的对比学习策略（SimCLR, MoCo 等）
   - 不同的数据增强策略
   - 集成多个模型的结果

## 总结

这次优化从**embedding 质量**入手，而不是直接调整 greedy 参数：
- ✅ 增强对比学习权重（1.0 → 1.5）
- ✅ 增强边重构损失（0.1 → 0.15）
- ✅ 保持其他优化不变
- ✅ 预期 F1 从 81.22% 提升到 83-87%

这是一个更根本的解决方案，直接解决 Precision 低的根源问题！
