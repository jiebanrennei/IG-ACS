# 同构图优化已实施 ✅

## 修改的文件

### 1. `train_ig.py` (核心修改)

**位置**：第 1318-1380 行（`set_everything(args.seed)` 之后）

**功能**：自动检测同构图数据集并应用优化参数

**优化内容**：

| 参数 | 默认值 | 优化值 | 作用 |
|------|--------|--------|------|
| `tau` | 0.4 | **0.25** | 降低温度，增强对比学习区分度 |
| `num_cand_per_node` | 5 | **10-15** | 增加候选边数量 |
| `cand_sources` | embed | **embed,twohop,common** | 多种候选边来源 |
| `lambda_cand_bce` | 0.0 | **0.1** | 增加重构损失权重 |
| `lambda_rec` | 0.1 | **0.05** | 降低重构保持损失 |
| `adv_lambda` | 1.0 | **1.2** | 增加对抗损失权重 |
| `adv_temp` | 1.0 | **0.8** | 边权重更尖锐 |
| `bias` | 0.0001 | **0.001** | 调整边权重偏置 |

**数据集特定优化**：
- **com-amazon**: 隐藏层 256，候选边 15
- **com-dblp**: 隐藏层 256，候选边 10
- **com-youtube/twitter/livejournal**: 隐藏层 128（避免 OOM）

## 使用方法

### 直接运行（自动优化）

```bash
# 同构图会自动应用优化配置
python train_ig.py --dataset com-amazon --encoder gcn --num_epochs 200

# 异构图不受影响
python train_ig.py --dataset ACM --encoder hii --num_epochs 200
```

### 手动覆盖参数（可选）

```bash
# 可以手动指定参数覆盖自动优化
python train_ig.py --dataset com-amazon --encoder gcn --tau 0.3 --num_cand_per_node 20
```

## 检测逻辑

```python
homogeneous_datasets = ['com-amazon', 'com-dblp', 'com-youtube', 'com-twitter', 'com-livejournal']
is_homogeneous = args.dataset.lower() in homogeneous_datasets

if is_homogeneous:
    # 应用优化参数
    args.tau = 0.25
    args.num_cand_per_node = 10
    # ... 其他优化
```

## 输出示例

运行同构图训练时，会看到：

```
============================================================
检测到同构图数据集: com-amazon
应用同构图特定优化配置...
============================================================
  [优化] tau: 0.4 → 0.25 (增强对比学习区分度)
  [优化] num_cand_per_node: 5 → 15
  [优化] cand_sources: embed → embed,twohop,common
  [优化] lambda_cand_bce: 0.0 → 0.1
  [优化] lambda_rec: 0.1 → 0.05
  [优化] adv_lambda: 1.0 → 1.2
  [优化] adv_temp: 1.0 → 0.8
  [优化] bias: 0.0001 → 0.001
  [优化] 隐藏层维度: 1024 → 256 (适配 Amazon 数据集)
  [优化] num_cand_per_node: 10 → 15 (Amazon 社区较大)
============================================================
```

## 不影响异构图

- ✅ 异构图数据集（ACM/DBLP/IMDB）不会触发优化
- ✅ 所有优化只在检测到同构图时应用
- ✅ 可以手动指定参数覆盖自动优化

## 预期效果

- **优化前**：com-amazon F1 ≈ 2.55%
- **优化后**：com-amazon F1 ≈ 15-30%（预期）
- **异构图**：性能保持不变

## 下一步

在服务器上运行：

```bash
# 测试 com-amazon
python train_ig.py --dataset com-amazon --encoder gcn --num_epochs 200

# 记录结果到 best_per/experiment_log.md
```
