# Precision 优化方案（第三轮 - 温和版）

## 问题分析

### 第二轮优化失败原因

第二轮结果（com-amazon）：
- **F1 = 69.11%** ❌ 比第一轮 81.22% 下降了 12%
- **Precision = 74.86%** ✅ 仅提升 0.59%
- **Recall = 70.77%** ❌ 从 94.42% 暴跌 23.65%

**核心问题**：参数过于激进，扩展过于保守
- `size_penalty=0.005` 惩罚太强
- `patience=10` 和 `min_gain_tol=0.01` 导致过早停止
- Recall 损失过大，Precision 提升微小

## 第三轮优化（温和版）

### 关键调整

| 参数 | 第二轮（过激） | 第三轮（温和） | 调整幅度 |
|------|--------------|--------------|---------|
| `greedy_size_penalty` | 0.005 | **0.001** | 降低 80% |
| `greedy_patience` | 10 | **3** | 降低 70% |
| `greedy_min_gain_tol` | 0.01 | **0.002** | 降低 80% |
| `greedy_balance_alpha` | 0.35 | **0.3** | 降低 14% |

### 优化参数详解

#### 1. 社区大小惩罚（温和版）
```python
greedy_size_penalty: 0.0 → 0.0005 → 0.001 (Amazon)
```
- 比第二轮降低 80%
- 提供轻微惩罚，避免过度扩展
- 不会大幅抑制 Recall

#### 2. 早停耐心值（宽松版）
```python
greedy_patience: 0 → 3
```
- 允许 3 步的波动
- 比 patience=10 宽松得多
- 避免过早停止

#### 3. 最小增益阈值（宽松版）
```python
greedy_min_gain_tol: 0.0 → 0.002
```
- 要求每次扩展至少提升 0.002
- 比 0.01 宽松 5 倍
- 允许更多节点加入

#### 4. balance_alpha（回调）
```python
greedy_balance_alpha: 0.35 → 0.3
```
- 从 0.35 回调到 0.3
- 保持与第一轮相同的值
- 避免过度强调 support

## 预期效果

### 对比表

| 指标 | 第一轮 | 第二轮（失败） | 第三轮预期 |
|------|-------|--------------|-----------|
| **Precision** | 74.27% | 74.86% | **78-82%** |
| **Recall** | 94.42% | 70.77% | **85-90%** |
| **F1** | 81.22% | 69.11% | **82-85%** |

### 目标
- Precision: 78-82%（显著提升）
- Recall: 85-90%（适度下降但保持高位）
- F1: 82-85%（超过第一轮的 81.22%）

## 测试命令

```bash
# 在服务器上运行（自动应用温和版优化）
python train_ig.py --dataset com-amazon --encoder gcn --num_epochs 200
```

## 优化信息输出

运行时会看到：
```
============================================================
检测到同构图数据集: com-amazon
应用同构图特定优化配置...
============================================================
  [优化] tau: 0.4 → 0.25 (增强对比学习区分度)
  [优化] num_cand_per_node: 5 → 15
  [优化] cand_sources: embed → embed,twohop,common
  [优化] lambda_cand_bce: 0.0 → 0.1
  [优化] lambda_rec: 0.1 → 0.05
  [优化] adv_lambda: 1.0 → 1.2
  [优化] adv_temp: 1.0 → 0.8
  [优化] bias: 0.0001 → 0.001
  [优化] greedy_balance_alpha: 0.15 → 0.3 (增强 Precision)
  [优化] suspicious_boost: 1.5 → 2.5 (增强可疑节点权重)
  [优化] greedy_size_penalty: 0.0 → 0.0005 (温和惩罚过大社区)
  [优化] greedy_patience: 0 → 3 (适度早停)
  [优化] greedy_min_gain_tol: 0.0 → 0.002 (温和最小增益)
  [优化] 隐藏层维度: 1024 → 256 (适配 Amazon 数据集)
  [优化] num_cand_per_node: 10 → 15 (Amazon 社区较大)
  [优化] greedy_size_penalty: 0.0005 → 0.001 (Amazon 轻微增强)
============================================================
```

## 技术原理

### 为什么第二轮失败？

**过度惩罚导致扩展不足**：
- size_penalty=0.005 意味着每增加一个节点，评分被扣减 0.005
- 如果节点贡献 < 0.005，就不会被加入
- 这导致很多有用的节点被排除
- Recall 从 94% 暴跌到 70%

**早停过于激进**：
- patience=10 意味着必须连续 10 步无改善才停止
- 但 min_gain_tol=0.01 要求每步至少提升 0.01
- 两者结合导致很多扩展路径被过早终止

### 第三轮的改进

**温和惩罚**：
- size_penalty=0.001 提供轻微惩罚
- 只有贡献 < 0.001 的节点才会被排除
- 大部分有用节点仍会被加入

**宽松早停**：
- patience=3 允许短暂波动
- min_gain_tol=0.002 要求较低
- 允许更多扩展路径

## 与基线方法的对比

### 基线方法（com-amazon）
- SEAL: 73.98%
- SLSS: 81.96%
- ComAF: 84.65%
- SLRL: 87.10%

### 我们的方法演进
- 第一轮：81.22%（接近 SLSS）
- 第二轮：69.11%（失败）
- **第三轮预期：82-85%**（接近 ComAF/SLRL）

## 下一步计划

1. ✅ **在服务器上运行测试**
2. ✅ **记录结果到 experiment_log.md**
3. ✅ **如果 F1 达到 82-85%，则成功**
4. ✅ **如果仍有差距，考虑微调参数**
5. ✅ **测试其他同构图数据集**

## 总结

第三轮优化采用温和策略：
- ✅ 大幅降低惩罚参数（size_penalty, patience, min_gain_tol）
- ✅ 保持 balance_alpha 在 0.3（与第一轮相同）
- ✅ 预期 F1 从 81.22% 提升到 82-85%
- ✅ 接近 SLRL (87.10%) 的性能水平
