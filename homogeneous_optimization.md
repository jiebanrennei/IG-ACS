# 同构图性能优化指南

## 当前问题
- 同构图（com-amazon 等）性能远低于异构图（ACM/DBLP/IMDB）
- com-amazon: F1=2.55% vs SLRL: 87.10%
- 异构图: F1=69-75%

## 核心差异

### 异构图优势
1. **多关系类型**：不同类型的边携带不同语义信息
2. **丰富的节点特征**：原生的节点属性特征
3. **元路径**：可以定义多种语义路径
4. **关系融合**：ICRA 机制可以融合多种关系

### 同构图挑战
1. **单关系类型**：只有一种边类型，信息单一
2. **缺乏节点特征**：只能使用结构特征（已优化到 8 维）
3. **社区结构复杂**：大规模社交网络的社区边界模糊
4. **对比学习困难**：缺乏语义信息，正负样本区分度低

## 优化方案

### 方案 1：增强对比学习的正负样本构造（推荐）

**问题**：同构图缺乏语义信息，传统的基于嵌入相似度的正负样本不够准确

**解决**：
1. **基于社区标签的正样本**：利用已知的社区标签构造更准确的正样本对
2. **基于结构洞的负样本**：识别不同社区之间的桥接节点作为难负样本
3. **动态难度采样**：根据训练进度动态调整正负样本的难度

**实现位置**：`ig_model.py` 的 `generate_candidate_edges` 方法

### 方案 2：同构图特定的边重要性学习

**问题**：当前边重要性学习主要针对异构图的多关系设计

**解决**：
1. **结构重要性**：基于共同邻居、Jaccard 相似度、Adamic-Adar 指数
2. **社区一致性**：边两端节点是否属于同一社区的概率
3. **意图相关性**：边与查询意图的结构相关性（而非语义相关性）

**实现位置**：`edge_importance.py` 添加同构图特定的边特征

### 方案 3：调整对比学习温度参数

**问题**：同构图的嵌入分布可能与异构图不同，需要不同的温度参数

**解决**：
- 异构图：tau=0.5（当前默认）
- 同构图：tau=0.2-0.3（更尖锐的分布，增强区分度）

**实现位置**：`train_ig.py` 添加数据集特定的参数配置

### 方案 4：增强的重构候选边策略

**问题**：同构图的重构视图可能不够有效

**解决**：
1. **基于模块度的候选**：优先重构能提升社区模块度的边
2. **基于核心度的候选**：优先连接高度核心节点
3. **基于意图分布的候选**：使用意图向量指导候选边选择

**实现位置**：`ig_model.py` 的候选边生成逻辑

### 方案 5：训练策略优化

**问题**：同构图可能需要不同的训练策略

**解决**：
1. **预训练阶段**：先在结构特征上做自监督预训练
2. **对比学习权重**：增加对比学习损失的权重
3. **重构学习权重**：适当降低重构学习权重（同构图重构困难）
4. **学习率调度**：使用更激进的学习率衰减

**实现位置**：`train_ig.py` 的训练循环

## 实施优先级

1. **高优先级**（立即实施）：
   - ✅ 方案 3：调整温度参数（最简单，可能有显著效果）
   - ✅ 方案 1：基于社区标签的正负样本构造

2. **中优先级**（验证后实施）：
   - 方案 2：同构图特定的边重要性
   - 方案 5：训练策略优化

3. **低优先级**（作为后续改进）：
   - 方案 4：增强的重构策略

## 验证计划

1. 在 com-amazon 上测试（最小数据集）
2. 对比优化前后的性能
3. 确保异构图性能不下降
4. 记录实验结果

## 预期效果

- 优化后 com-amazon F1: 2.55% → 15-30%
- 仍然低于 SLRL（87%），但有显著提升
- 异构图性能保持不变或略有提升
